name: Deploy to cPanel

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.CPANEL_SSH_KEY }}" > ~/.ssh/cpanel_key
          chmod 600 ~/.ssh/cpanel_key

      - name: Upload files to cPanel
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/cpanel_key -p 2223 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            --exclude ".git" \
            --exclude ".github" \
            --exclude ".next" \
            --exclude "node_modules" \
            --exclude ".env" \
            --exclude ".env.*" \
            ./ "crak4345@serayu.iixcp.rumahweb.net:/home/crak4345/crawlingnich/"

      - name: Install, build, and restart app
        run: |
          ssh -i ~/.ssh/cpanel_key -p 2223 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null "crak4345@serayu.iixcp.rumahweb.net" << 'EOF'
            set -e

            source /home/crak4345/nodevenv/crawlingnich/20/bin/activate
            cd /home/crak4345/crawlingnich

            rm -rf .next

            npm ci
            npm run build

            mkdir -p tmp
            touch tmp/restart.txt
          EOF